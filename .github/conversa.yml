name: conversa

on: 
  push:
    branches:
      - main

jobs:
     escreve-commit:
         runs-on: ubuntu-latest

         steps:
             - name: Nosso primeiro workflow
               uses: echo ¨Nosso primeiro workflow¨
               